# 2026-05-18

## Markdown formatting rule

- Added `library/rules/formatting-markdown.md` for Markdown-only formatting guidance.
- Scoped the rule to `**/*.md` and `**/*.markdown`; it references the existing `.devops/markdown-format` markdownlint setup.
- Revised the rule to stay repo-agnostic and explicitly preflight common `markdownlint` failures before running lint.

## Markdownlint command files

- Updated `.markdownlint.yaml` so `library/commands/*.md` does not enforce `MD041/first-line-heading`, since command files intentionally start with YAML frontmatter.
- Fixed remaining markdownlint structure issues in `library/skills/README.md` and `library/skills/creator-skill/agents/analyzer.md`.
- Split benchmark analysis guidance from `agents/analyzer.md` into `agents/benchmark-analyzer.md` because it is a peer agent prompt, not a subsection.
