# 2026-06-09

- Consolidated the `review-code` skill into a self-contained `SKILL.md` body
  covering review workflow, risk areas, finding standards, findings-first
  output, review rules, and verification.
- Simplified `review-code` by removing the standalone review-focus section and
  merging its essential checks into the workflow and review rules.
- Added a three-pass review process, compact risk dimensions, and review
  anti-patterns to `review-code` based on `code-review.md`.
- Added conditional verification checklists for each `review-code` dimension.
- Compressed duplicated workflow, checklist, finding, output, rule, and
  verification guidance in `review-code`; removed the stale README reference to
  its deleted `references/` directory.
- Adapted the manually revised `review-code` checklists to restore compatibility,
  rollout, operations, accessibility, documentation, and agent-security
  coverage while removing technology-specific prescriptions.
- Generalized JavaScript and web-specific checklist labels in `review-code` and
  clarified that checklist order follows the changed surface, prioritizing
  trust-boundary, data-loss, and availability risks.
- Added an implementation-ready user story for the `markitdown` skill covering
  CLI-only local single-file conversion, temporary model output, explicit user
  output, session-local reuse, dependency consent, and failure handling.
- Narrowed automatic `markitdown` conversion to extraction-dependent formats:
  PDF, Word, PowerPoint, Excel, Outlook, and EPUB. Agents should read HTML, CSV,
  JSON, XML, and plain text directly when supported.
- Replaced the `write-tech-docs` router with five standalone documentation
  skills: `write-api-docs`, `write-changelog`, `write-readme`,
  `write-release-notes`, and `write-runbook`.
- Combined routine and on-call operational guidance in `write-runbook` using
  explicit mode detection; kept the other documentation jobs independent
  because their audiences, evidence, structures, and verification differ.
