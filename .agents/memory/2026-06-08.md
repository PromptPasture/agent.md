# 2026-06-08

- Updated the `plan` skill to keep discussion drafts conversational and save
  plans at `docs/YYYY-MM-DD-[topic]/PLAN.md` only after explicit approval or
  an explicit save request; bumped its version to `1.4.0`.
- Completed the `audit-skill-security` body with a read-only security audit
  workflow, severity model, install verdicts, error paths, and verification.
- Restored metadata and typosquat checks, static text normalization, dangerous
  permission-combination rules, update diffs, and examples after review.
- Split `manage-git` into focused `utility/git-branch` and
  `utility/git-commit` skills, and updated the skill index and Git message
  command reference.
- Adapted the new Git skills to the repository's skill-building guide by
  compacting Markdown tables, clarifying branch separators, and adding complete
  commit-message examples; triggering and functional tests were excluded by
  request.
- Moved `write-prd`, `write-spec`, and `write-user-story` into the `product`
  catalog and moved `write-tech-docs` into the `documentation` catalog.
- Completed the self-contained `write-user-story` body with its workflow,
  output template, writing rules, error paths, and verification guidance.
- Made the user-story Dependencies and Open Questions section optional.
- Simplified the user-story template by making Context, Scope, Developer Tasks,
  and Dependencies and Open Questions optional and folding error paths into
  the writing rules.
- Removed Context, Scope, and Developer Tasks from the default user-story
  template; add them only when needed for an implementation-ready handoff.
