# 2026-05-15 (UTC)

## Context

Skill generation, backend implementation, and small-task memory conventions matured.

## Observations

- Moved small task notes into `.agents/memory/YYYY-MM-DD.md`.
- Added `design-api` as a router skill with OpenAPI, AsyncAPI, and GraphQL references.
- Added the Agent PDLC diagram in `pages/AGENT_PDLC.md`.
- Added `codegen-test` and `codegen-backend` skills.
- Split `create-skill` references and simplified skill descriptions.
- Added skill length budgets to playbook docs: metadata <= 100 tokens and instructions <= 5000 tokens.
- Tracked `evals.json` while ignoring other skill eval contents.
