# 2026-05-16 (UTC)

## Context

Frontend and test-generation skills were expanded into routed reference structures.

## Observations

- Split backend and frontend code-generation references into language and framework routing layers.
- Added eval and improvement scripts to `codegen-test`.
- Built `codegen-frontend` as a router skill with language, framework, styling, and frontend capability references.
- Added `css-component-libraries`, JavaScript framework reference prefixes, and an `html` markup reference for plain HTML, templates, static pages, metadata, and progressive enhancement.
