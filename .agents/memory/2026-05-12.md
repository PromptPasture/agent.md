# 2026-05-12 (UTC)

## Context

Git command skills and the reference implementation layout were simplified.

## Observations

- Added `git-branch` and `git-message` commands.
- Renamed Git-related skills to `git-*`, then consolidated them into a router skill with references.
- Promoted root `AGENTS.md` as the primary entry point and clarified its purpose in playbook docs.
- Moved `.agents` content to `examples` and flattened the rules, commands, and agents layout.
- Removed eval JSON files from tracking and added them to `.gitignore`.
- Updated the agent-playbook spec to use the `AGENTS.md` entry point and a concrete folder layout.
- Marked optional sections in `writer-prd`.
