# 2026-05-18

## Markdown formatting rule

- Added `.agents/rules/formatting-markdown.md` for Markdown-only formatting guidance.
- Scoped the rule to `**/*.md` and `**/*.markdown`; it references the existing `.devops/markdown-format` markdownlint setup.
- Revised the rule to stay repo-agnostic and explicitly preflight common `markdownlint` failures before running lint.

## Markdownlint command files

- Updated `.markdownlint.yaml` so `.agents/commands/*.md` does not enforce `MD041/first-line-heading`, since command files intentionally start with YAML frontmatter.
- Fixed remaining markdownlint structure issues in `.agents/skills/README.md` and `.agents/skills/creator-skill/agents/analyzer.md`.
- Split benchmark analysis guidance from `agents/analyzer.md` into `agents/benchmark-analyzer.md` because it is a peer agent prompt, not a subsection.

## Markdown formatting workflow

- [x] Updated `.github/workflows/markdown-format.yml` to run markdownlint against all Markdown files instead of only changed files.
- [x] Refreshed `.devops/markdown-format/README.md` so its CI section matches the workflow.
- [x] Excluded installed dependency Markdown from the all-files glob and fixed existing first-party markdownlint findings.
- [x] Removed obsolete changed-file markdownlint helper scripts from `.devops/markdown-format`.
- [x] Normalized Markdown tables to compact `MD060` style and updated `.agents/rules/formatting-markdown.md` to require compact table separators.

## Always-on guidance consolidation

- [x] Moved `artifact-quality` and `token-efficiency` guidance into root `AGENTS.md`.
- [x] Removed the separate library rule files so `AGENTS.md` is the source of truth for always-on behavior.

## Release and JS tooling

- [x] Added tag-triggered release workflow support with `git-cliff` via `.github/workflows/release.yml` and `cliff.toml`.
- [x] Consolidated markdown tooling into `.devops/js-tools`, moving the package lock from `.devops/markdown-format` and replacing the old markdown-only package setup.
- [x] Added JS tooling scripts for markdownlint and token counting, then renamed the token tally command/script to `token-count`.
- [x] Added `.github/workflows/library-token-diff.yml` to comment library token diffs on PRs that change library content.

## Skills and team-role docs

- [x] Added the `how` skill for codebase exploration and explanation, including eval coverage.
- [x] Added the `review-code` skill for code review workflows, including evals and focused references for regressions, security, performance, test gaps, and review checklists.
- [x] Updated the team-roles-as-skills PRD, spec, and tasks to rename `checklist-code-review` to `review-code`, add review as a skill type, consolidate skill catalog entries through multi-variant routers, and fold `template-pr`/`template-retro` into `template-creator`.

## Pages and README

- [x] Clarified the Pages playbook/index distinction between `rules/` content and root `AGENTS.md` guidance.
- [x] Cleaned up README badges and added `pages/assets/landing.jpeg` for the landing page.
- [x] Added a custom emoji favicon through `pages/_config.yml` and `pages/_includes/head-custom.html`.
