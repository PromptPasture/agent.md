# 2026-06-11

- Updated the brainstorm skill output contract: `BRAINSTORM.md` is a general brainstorming record rather than a technical design. Its frontmatter uses required `topic`, `method`, and `date` fields plus optional `related` references. The skill writes the document but does not commit changes.
