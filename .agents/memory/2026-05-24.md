# 2026-05-24

## Metadata categories

- Added `metadata.category` to maintained skills, rules, and commands.
- Updated `create-skill` and `create-rule` authoring guidance so new artifacts place categories under `metadata.category`.
- Kept category values lowercase kebab-case for compatibility with issue #55-style category filtering while preserving the repo-local `metadata.*` convention.

## Document frontmatter convention

- Moved generated document metadata guidance from body tables/`Document Info` blocks into YAML frontmatter across `write-prd`, `write-spec`, `write-tech-docs`, and `write-user-story` templates.
- Use quoted date-like values, bracketed placeholders in templates, no-space status tokens, and optional fields only when they have real values.
