# 2026-05-24 (UTC)

## Context

Skill naming, metadata, document metadata, and utility skill coverage were aligned.

## Observations

- Renamed skills to a verb-first naming convention, including `write-tests` to `build-tests`.
- Added general utility skills: `ask-questions`, `classify-content`, `coordinate-work`, `decide-direction`, `explore-context`, `plan-work`, `reason-problem`, `remember-context`, and `adapt`.
- Added tags and YAML frontmatter metadata to skills.
- Moved `author`, `version`, and `source` under `metadata`; added `metadata.category` to maintained skills, rules, and commands.
- Updated `create-skill` and `create-rule` authoring guidance so new artifacts place categories under `metadata.category`.
- Replaced `Scope` sections with `Boundaries` after `Workflow` in skills.
- Updated `create-skill` so `references/authoring.md` is the source of truth for skill authoring details; new skill bodies should follow the compact default shape: title, one-sentence purpose, `Workflow`, `Output`, `Boundaries`, optional `Error Paths`, and `Verification`.
- Moved generated document metadata guidance from body tables and `Document Info` blocks into YAML frontmatter across `write-prd`, `write-spec`, `write-tech-docs`, and `write-user-story` templates.
- Excluded YAML frontmatter from Markdown token tallies.
- Updated team-roles-as-skills docs with variant naming, audit-prefix guidance, formal SPEC sections, decisions, and release gates.
