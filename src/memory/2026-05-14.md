# 2026-05-14 (UTC)

## Context

Completed task-only docs were collected into memory so `docs/` can stay focused on project docs and durable task folders.

## Observations

- Added `create-skill`, `create-rule`, `operator-git`, and token-efficiency guidance.
- Generalized `create-skill` for multiple coding agents and configurable runner options where practical.
- Added create-rule eval prompts, local validation, trigger evals, output grading scripts, and validation workflow docs.
- Moved GitHub Pages files into `pages/`, updated repository links, and added a path-filtered Pages workflow.
- Added Markdown formatting CI for changed Markdown files.
- Consolidated database design skill coverage.
- Updated `.gitignore` so `examples/**/evals/` is ignored instead of only `evals.json`.
